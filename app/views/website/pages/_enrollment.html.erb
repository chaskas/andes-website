<section id="contacto" class="enrollment-section">
  <div class="container">
    <div class="row g-5 align-items-start">
      <%# Introductory text %>
      <div class="col-lg-5 reveal">
        <span class="section-label"><%= t('website.nav.contact') %></span>
        <h2 class="section-title mb-4"><%= t('website.enrollment.title') %></h2>
        <p class="enrollment-desc"><%= t('website.enrollment.p1') %></p>
        <p class="enrollment-desc"><%= t('website.enrollment.p2') %></p>
        <p class="enrollment-desc">
          <%= link_to t('website.enrollment.trial_cta'), website.trial_classes_path, class: "btn btn-andes mt-2" %>
        </p>
        <p class="enrollment-welcome"><%= t('website.enrollment.welcome') %></p>
      </div>

      <%# Enrollment form %>
      <div class="col-lg-7 reveal">
        <div class="enrollment-form-card">
          <% if flash[:notice] %>
            <div class="alert alert-success d-flex align-items-center" role="alert">
              <i class="bi bi-check-circle-fill me-2"></i>
              <%= flash[:notice] %>
            </div>
          <% end %>
          <% if flash[:alert] %>
            <div class="alert alert-danger d-flex align-items-center" role="alert">
              <i class="bi bi-exclamation-circle-fill me-2"></i>
              <%= flash[:alert] %>
            </div>
          <% end %>

          <%= form_with model: @enrollment || Website::Enrollment.new,
                        url: website.enrollments_path,
                        local: true,
                        class: "form-andes" do |f| %>

            <% if @enrollment&.errors&.any? %>
              <div class="alert alert-danger">
                <ul class="mb-0">
                  <% @enrollment.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <div class="row g-3">
              <div class="col-md-6">
                <%= f.label :contact_name, "#{t('website.enrollment.form.contact_name')} *", class: "form-label" %>
                <%= f.text_field :contact_name, class: "form-control", placeholder: t('website.enrollment.form.placeholders.contact_name'), required: true %>
              </div>
              <div class="col-md-6">
                <%= f.label :email, "#{t('website.enrollment.form.email')} *", class: "form-label" %>
                <%= f.email_field :email, class: "form-control", placeholder: t('website.enrollment.form.placeholders.email'), required: true %>
              </div>
            </div>

            <div class="mt-3">
              <%= f.label :phone, t('website.enrollment.form.phone'), class: "form-label" %>
              <%= f.telephone_field :phone, class: "form-control", placeholder: t('website.enrollment.form.placeholders.phone') %>
            </div>

            <div class="mt-3">
              <%= f.label :comments, "#{t('website.enrollment.form.comments')} *", class: "form-label" %>
              <%= f.text_area :comments, class: "form-control", rows: 3, placeholder: t('website.enrollment.form.placeholders.comments'), required: true %>
            </div>

            <div class="mt-3">
              <div class="form-check">
                <%= f.check_box :privacy_accepted, class: "form-check-input", required: true %>
                <%= f.label :privacy_accepted, class: "form-check-label" do %>
                  <%= t('website.enrollment.form.privacy_accepted',
                        link: link_to(t('website.enrollment.form.privacy_link_text'),
                                      website.datenschutz_path,
                                      target: "_blank",
                                      rel: "noopener")).html_safe %>
                <% end %>
              </div>
            </div>

            <div class="mt-4">
              <%= f.submit t('website.enrollment.form.submit'), class: "btn-submit" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</section>
